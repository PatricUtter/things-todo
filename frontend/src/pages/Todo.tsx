import React from 'react';

export interface ITodoPageProps {}

const TodoPage: React.FunctionComponent<ITodoPageProps> = (props) => {
    return (
        <div>
            <h1>TodoList with Uttern</h1>
        </div>
    );
};

export default TodoPage;
